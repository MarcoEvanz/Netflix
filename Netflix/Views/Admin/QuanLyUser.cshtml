
@{
    ViewBag.Title = "QuanLyUser";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@model IEnumerable<Netflix.Models.KhachHang>

<style>
    h2 {
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 20px;
        color: #333; /* Màu chữ đen */
    }

    p {
        margin-bottom: 20px;
    }

    .table {
        width: 100%;
        border-collapse: collapse;
    }

        .table th {
            background-color: #e50914; /* Màu đỏ của Netflix */
            color: #fff; /* Màu chữ trắng */
            font-weight: bold;
            padding: 10px;
            text-align: left;
        }

        .table td {
            border: 1px solid #ccc; /* Đường viền màu xám */
            padding: 10px;
        }

        .table tr:nth-child(even) {
            background-color: #f2f2f2; /* Màu nền xám nhạt cho hàng chẵn */
        }

        .table tr:nth-child(odd) {
            background-color: #fff; /* Màu nền trắng cho hàng lẻ */
        }

        .table a {
            color: #e50914; /* Màu đỏ của Netflix cho liên kết */
            text-decoration: none;
            margin-right: 10px;
        }

            .table a:hover {
                text-decoration: underline; /* Hiệu ứng gạch chân khi di chuột qua liên kết */
            }
</style>
<h2>QuanLyPhim</h2>

<p>
    @Html.ActionLink("Thêm User", "AddKhachHang")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HoTenKH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenDangNhap)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HoTenKH)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenDangNhap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
            <td>
                @Html.ActionLink("Edit", "SuaKhachHang", new { id = item.MaKH }) |
                @Html.ActionLink("Delete", "XoaKhachHang", new { id = item.MaKH })
            </td>
        </tr>
    }
</table>

